# ─── Gn01Cms Runtime Container (pre-built publish output) ──
FROM docker.io/library/dotnet:10.0-aspnet
WORKDIR /app
COPY . /app
ENV ASPNETCORE_ENVIRONMENT=Production
# CMS_ADMIN_PASSWORD is set at runtime via podman -e
EXPOSE 5000
ENTRYPOINT ["dotnet", "Gn01Cms.Web.dll"]
